package LeetCode;

import java.util.Arrays;

public class SingleNumber {
    /*public int numberCounter(int[] nums) {
        int index =0;

        boolean valRepeated = true;
        Arrays.sort(nums);
        for(int i =0;i<nums.length-1;i++){
            if(nums[i]!= nums[i+1] && !valRepeated){
                valRepeated = false;
                index =i;
                break;
            }else {
                valRepeated = true;
            }
        }
        if(nums[nums.length-1] != nums[nums.length-2] ){
            return nums[nums.length-1]
        }
        return 0;
    }*/
}
